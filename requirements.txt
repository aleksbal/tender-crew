pymupdf>=1.23.0
python-docx>=0.8.11

# PII anonymization (Presidio)
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0
spacy>=3.4.0

# Testing / runtime
pytest>=7.0
requests>=2.0
jsonschema>=4.0
openai>=1.0

# Optional / dev tools (uncomment to install for development)
# black>=23.0
# isort>=5.0
# pre-commit>=3.0
# mypy>=1.0

# Tools (install via system package manager):
# - qpdf (useful for PDF tooling)
# - ghostscript / poppler-utils (PDF handling utilities)
# - pandoc for DOCX -> structured JSON AST (optional)
