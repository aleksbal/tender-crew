Extract and structure the following CV/resume document according to the JSON schema provided. Return exactly ONE JSON object.

DOCUMENT TEXT:
{DOCUMENT_TEXT}

JSON SCHEMA:
{SCHEMA_TEXT}

INSTRUCTIONS:
- Do not assume any layout (no columns, headers, or table markers are guaranteed).
- Detect sections using language cues and content (German/English), then map them to the schema.
- Use "YYYY-MM" or "present" per the system rules.
- For required fields with missing data, use "" or [].
- Apply COVERAGE POLICY and PROJECTS POLICY from system rules.
- Ensure each project's tools_and_frameworks sums to 100.
- Output ONLY valid JSON, no markdown, no explanations.

